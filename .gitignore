# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cloudflare
.wrangler/

# Written from .env.local at build and deploy time, never committed — see
# scripts/mapbox-config.mjs. Keeping the Mapbox token out of the bundle keeps
# it out of the frozen releases, which are committed and never rebuilt.
public/mapbox.json
releases/mapbox.json
releases/**/mapbox.json
